export enum TIMES {
  NOTIFICATION_TIME = 4,
  TERMINATION_TIME = 5
}

export enum HTTPRESPONSE {
  NOT_VALID_JSON = "Invalid JSON"
}

export enum ERRORS {
  NO_BRANCH = "'BRANCH' environment variable not found"
}
